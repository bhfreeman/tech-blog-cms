<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <!-- CSS only -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
</head>

<body>
  <header class="container mx-0 mb-5">
    <nav class="row">
      <div class="col-12">
      <a href="/" class="fs-3">Home</a>
      <a href="/dash" class="fs-3">Dashboard</a>
      {{#if logged_in}}
      <button id="logout" class="fs-3">Logout</button>
      {{else}}
      <a href="/login" class="fs-3">Login</a>
      {{/if}}
      </div>
    </nav>
    <div class="row justify-content-start">
      <div class="col-4">
      <span class="badge bg-info fs-1">The Tech Blog</span>
      </div>
    </div>
  </header>
  <main class="container">
    <div class="row">
    {{{ body }}}
    </div>
  </main>
  <footer>
      
  </footer>
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
