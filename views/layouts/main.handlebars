<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <!-- CSS only -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
</head>

<body>
  <header class="mb-3">
    {{!-- <nav class="row">
      <div class="col-12">
        <a href="/" class="fs-3">Home</a>
        <a href="/user" class="fs-3">Dashboard</a>
        {{#if logged_in}}
        <button id="logout" class="fs-3">Logout</button>
        {{else}}
        <a href="/login" class="fs-3">Login</a>
        {{/if}}
      </div>
    </nav> --}}
    {{!-- <div class="row justify-content-start">
      <div class="col-4">
        <span class="badge bg-info fs-1">The Tech Blog</span>
      </div>
    </div> --}}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">The Tech Blog</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
          aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/user">Dashboard</a>
            </li>
            {{#if logged_in}}
            <li class="nav-item">
              <button id="logout" class="btn btn-dark">Logout</button>
            </li>
            {{else}}
            <li class="nav-item">
              <a class="nav-link" href="/login">Login</a>
            </li>
            {{/if}}
          </ul>
        </div>
      </div>
    </nav>
  </header>
  <main class="container">
    <div class="row">
      {{{ body }}}
    </div>
  </main>
  <footer>

  </footer>
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>