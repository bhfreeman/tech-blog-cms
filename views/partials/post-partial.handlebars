<div class="row col-12 text-center border border-dark m-0">
    <div class="col-12 row border border-dark m-0 justify-content-start"></div>
        <h3 class="col-4 fs-6 w-25"> {{format_date createdAt}}</h3>
        <a href="/user/{{user.id}}" class="col-4 fs-4" >User: {{user.name }}</a>
    </div>
    <h1>{{ title }}</h1>
    <p> {{ post_text }}</p>
</div>