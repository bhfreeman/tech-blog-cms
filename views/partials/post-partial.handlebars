<div class="row col-12 text-center border border-dark">
    <div class="col-12 border border-dark">
        <a href="/user{{user.id}}">User: {{user.name }}</a>
        <h3> {{format_date createdAt}}</h3>
    </div>
    <h1>{{ title }}</h1>
    <p> {{ post_text }}</p>
</div>